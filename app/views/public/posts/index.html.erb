#そのタグに紐づく投稿が何件あるかをカウントしている
<diV>
  <% @tags.each do |tag| %>
    <%= tag.name %>
    <%= "(#{tag.posts.count})" %>
  <% end %>
</diV>

#投稿には複数のタグが存在し配列である。その中から要素を抜き出してそれぞれ表示させるためのものがmap(&:name)であり、.join(',')で区切っている。
<div>
  <% @posts.each do |post| %>
    <div>
      <%= link_to post.title, public_path(post.id) %>
    </div>
    <div>
      <i><%= post.tags.map(&:name).join(',') %></i>
    </div>
  <% end %>
</div>
